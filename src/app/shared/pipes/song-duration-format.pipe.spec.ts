import { SongDurationFormatPipe } from './song-duration-format.pipe';

describe('SongDurationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SongDurationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
